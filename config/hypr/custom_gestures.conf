# Workspace swipe settings
gestures {
    workspace_swipe_distance = 300
    workspace_swipe_invert = true
    workspace_swipe_touch = false
    workspace_swipe_touch_invert = false

# Prevent fast swipes from forcing an instant transition
    workspace_swipe_min_speed_to_force = 1000000

# workspace_swipe_min_speed_to_force = 30
    workspace_swipe_cancel_ratio = 0.5
    workspace_swipe_direction_lock = true
    workspace_swipe_direction_lock_threshold = 10
    workspace_swipe_create_new = true
    workspace_swipe_forever = false
    workspace_swipe_use_r = false
    close_max_timeout = 1000
}

# Three-finger horizontal swipe for workspace switching
gesture = 3, horizontal, workspace

# Four-finger horizontal swipe for workspace switching
gesture = 4, horizontal, workspace
